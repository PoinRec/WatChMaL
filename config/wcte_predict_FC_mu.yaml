defaults:
  - data: wcte_calibration_run
  - model: resnet50
  - engine: FCclassifier
  - tasks/restore_state: restore_FC_mu
  - tasks/predict: test
  - _self_

hydra:
  run:
    dir: /home/zhihao/Data/WCTE_calibration_run/Predict/FC_mu/
  job:
    chdir: True


data:
  dataset:
    default_transformation: true
    use_new_mpmt_convention: true
  split_path: /home/zhihao/Data/WCTE_calibration_run/dummy_split_list.npz

model:
  num_output_channels: 2

gpu_list: [0, 1, 2, 3] # test on 4 GPUs
seed: 42
dump_path: './outputs/'