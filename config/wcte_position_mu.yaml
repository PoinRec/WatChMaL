defaults:
  - wcte_defaults
  - engine: regression
  - override loss@tasks.train.loss: huber
  - _self_

engine:
  target_key:
    - positions
  target_scale_factor:
    positions: 1

model:
  num_output_channels: 3

hydra:
  run:
    dir: /home/zhihao/Data/WCTE_data_fixed/Output/position_mu_${now:%Y-%m-%d_%H:%M:%S}/

data:
  split_path: /home/zhihao/Data/WCTE_data_fixed/split_list_mu.npz

tasks:
  train:
    epochs: 150
    loss:
      delta: 15
