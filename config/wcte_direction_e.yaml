defaults:
  - wcte_defaults
  - engine: regression
  - override loss@tasks.train.loss: huber
  - _self_

engine:
  target_key:
    - directions
  target_scale_factor:
    directions: 0.01

model:
  num_output_channels: 3

hydra:
  run:
    dir: /home/zhihao/Data/WCTE_data_fixed/Output/direction_e_${now:%Y-%m-%d_%H:%M:%S}/

data:
  split_path: /home/zhihao/Data/WCTE_data_fixed/split_list_e.npz


tasks:
  train:
    epochs: 80
    loss:
      delta: 10



